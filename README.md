# AI vs Human 텍스트 탐지기

이 프로젝트는 전통적인 머신러닝(TF-IDF + SVM)과 딥러닝(RoBERTa) 접근 방식을 모두 사용하여 AI가 생성한 텍스트를 탐지하는 종합적인 솔루션을 제공합니다. LIME을 사용한 실시간 분석 및 설명 가능성(XAI)을 위한 대화형 웹 인터페이스가 포함되어 있습니다.

## 🚀 주요 기능

-   **이중 모델 아키텍처**:
    -   **CPU 모드**: 가볍고 빠름. 텍스트 분류에 **TF-IDF + SVM**(98.35% 정확도)을 사용하고 특징 중요도 분석에 **Random Forest**를 사용합니다.
    -   **GPU 모드**: 고성능. 최첨단 탐지를 위해 파인튜닝된 **RoBERTa**(98.55% 정확도)를 사용합니다.
-   **설명 가능한 AI (XAI)**:
    -   **LIME (Local Interpretable Model-agnostic Explanations)**: 어떤 단어가 결정에 가장 많이 기여했는지 시각화합니다.
    -   **언어학적 특징 분석**: 버스티니스, 퍼플렉서티, 가독성과 같은 특징을 분석하여 AI 텍스트를 구별합니다.
-   **대화형 웹 인터페이스**: Flask로 구축되어 사용자가 텍스트를 입력하고 시각화와 함께 즉각적인 예측을 받을 수 있습니다.

## 📂 프로젝트 구조

```
ai_vs_human_text/
├── data/                          # 데이터셋 및 처리된 파일
├── models_trained/                # 학습된 모델
│   ├── cpu_models/                # SVM, RF, Vectorizer (pkl 파일)
│   └── gpu_transformer/           # 파인튜닝된 RoBERTa 모델
├── notebooks/                     # EDA용 Jupyter 노트북
├── src/                           # 소스 코드
│   ├── app/                       # Flask 웹 애플리케이션
│   │   ├── web_app.py             # 애플리케이션 진입점
│   │   ├── templates/             # HTML 템플릿
│   │   └── static/                # 정적 자산 (CSS, JS)
│   ├── data/                      # 데이터 처리 모듈
│   │   ├── data_preprocessing.py  # 텍스트 전처리
│   │   └── feature_extraction.py  # 특징 엔지니어링
│   ├── models/                    # 모델 정의
│   │   └── model.py               # RoBERTa 모델 클래스
│   ├── pipelines/                 # 학습 및 평가
│   │   ├── train_all_models.py    # 모든 CPU 모델 학습
│   │   ├── train_transformer.py   # RoBERTa 학습
│   │   └── evaluate_models.py     # 모델 비교
│   └── eda/                       # 탐색적 데이터 분석
│       ├── perform_english_eda.py # EDA 스크립트
│       └── feature_engineering_pipeline.py
├── requirements.txt               # Python 의존성
└── README.md                      # 프로젝트 문서
```

## 🛠️ 설치 방법

1.  **저장소 복제**:
    ```bash
    git clone https://github.com/yourusername/ai_vs_human_text.git
    cd ai_vs_human_text
    ```

2.  **가상 환경 생성** (권장):
    ```bash
    conda create -n ai_detector python=3.10
    conda activate ai_detector
    ```

3.  **의존성 설치**:
    ```bash
    pip install -r requirements.txt
    ```

4.  **데이터 다운로드**:
    -   `AI_Human.csv` 데이터셋을 `data/` 디렉토리에 배치하세요.

## ⚡ 빠른 시작

### 1. 모델 학습 (아직 학습되지 않은 경우)
웹 앱에 필요한 모델 파일을 생성하려면:

```bash
# CPU 모델 학습 (SVM, RF 등)
python src/pipelines/train_all_models.py

# GPU 모델 학습 (선택사항, CUDA 필요)
python src/pipelines/train_transformer.py
```

### 2. 웹 애플리케이션 실행
```bash
python src/app/web_app.py
```
브라우저를 열고 `http://127.0.0.1:5000`으로 이동하세요.

## 📊 모델 성능

| 모델 | 정확도 | 정밀도 | 재현율 | F1-점수 |
| :--- | :--- | :--- | :--- | :--- |
| TF-IDF + SVM | 0.9835 | **0.9907** | 0.9661 | 0.9782 |
| **RoBERTa** | **0.9855** | 0.9780 | **0.9844** | **0.9812** |
| TF-IDF + LR | 0.9680 | 0.9796 | 0.9362 | 0.9574 |
| Feature + RF | 0.8710 | 0.8612 | 0.7917 | 0.8250 |

*참고: TF-IDF + SVM은 속도와 정확도의 뛰어난 균형으로 인해 CPU 모드의 기본값입니다.*

## 🔍 작동 원리

1.  **전처리**: 텍스트가 정제되고 토큰화됩니다.
2.  **특징 추출**: 언어학적 특징(엔트로피, 버스티니스 등)이 계산됩니다.
3.  **예측**: 토큰화된 입력이 RoBERTa 트랜스포머로 전달됩니다.
4.  **설명**: LIME은 입력을 교란하고 모델 변화를 관찰하여 로컬 설명을 생성합니다.

## 📜 라이선스

이 프로젝트는 MIT 라이선스에 따라 라이선스가 부여됩니다.
